# Toolchain pin — `mise install` gives every contributor the same Rust.
# Keep this on the latest stable; bump deliberately, commit mise.lock with it.
[tools]
rust = "1.97.1"

[settings]
lockfile = true
